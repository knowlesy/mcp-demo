apiVersion: v1
kind: Service
metadata:
  name: dummy-webapp
  namespace: apps
  labels:
    app: dummy-webapp
spec:
  selector:
    app: dummy-webapp
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
